#pragma once

#include <inttypes.h>

const size_t NUM_PIXELS_PER_DRAM_LINE = 16;
const size_t COMP_BLOCK_DRAM_OFFSET = 16384;
const size_t IMAGEWIDTH = 816;
const size_t NPIXELS = 5;

const uint16_t N = 5; // Number of single points in parallel
